-- Flyway V1: Baseline schema for define_db
-- This migration is a placeholder for the existing schema.
-- The actual tables are created by create_all_tables.sql.
-- Run with flyway.baseline-on-migrate=true on existing databases.

SELECT 1;
