# Define XML 后端开发设计文档

## 1. 技术架构

### 1.1 技术栈
- **框架**: Spring Boot 2.7+
- **数据访问**: MyBatis 3.5+
- **数据库**: MySQL 8.0+
- **Java版本**: JDK 17+
- **构建工具**: Maven 3.8+
- **文件处理**: Apache POI, SAS XPT Library
- **JSON处理**: Jackson 2.15+
- **日志**: SLF4J + Logback
- **测试**: JUnit 5 + Mockito

### 1.2 模块架构
```
stat-parent/
├── stat-common/           # 公共模块
│   ├── dto/              # 数据传输对象
│   ├── entity/           # 实体类
│   ├── enums/            # 枚举类
│   ├── exception/        # 异常类
│   └── result/           # 统一返回结果
├── stat-dal/             # 数据访问层
│   ├── mapper/           # MyBatis Mapper接口
│   └── config/           # 数据源配置
├── stat-service/         # 业务逻辑层
│   ├── impl/             # 服务实现类
│   ├── config/           # 业务配置
│   └── utils/            # 工具类
├── stat-web/             # Web控制层
│   ├── controller/       # 控制器
│   ├── config/           # Web配置
│   └── interceptor/      # 拦截器
└── stat-starter/         # 启动模块
    ├── config/           # 启动配置
    └── Application.java  # 启动类
```

## 2. 数据模型设计

### 2.1 核心实体类

#### 2.1.1 项目实体 (Project)
```java
@Data
public class Project {
    private Long id;
    private String projectId;           // 项目ID
    private String projectName;         // 项目名称
    private String protocolNumber;      // 方案编号
    private String protocolName;        // 方案名称
    private String protocolVersion;     // 方案版本
    private LocalDate protocolDate;     // 方案版本日期
    private String sponsor;             // 申办方/赞助方
    private String username;            // 用户名
    private Integer status;             // 状态：1-活跃，0-归档
    private LocalDateTime createdTime;  // 创建时间
    private LocalDateTime updatedTime;  // 更新时间
    private Integer deleted;            // 删除标记：0-未删除，1-已删除

    public Project() {}

    public Project(String projectId, String projectName) {
        this.projectId = projectId;
        this.projectName = projectName;
        this.status = 1;
        this.deleted = 0;
        this.createdTime = LocalDateTime.now();
        this.updatedTime = LocalDateTime.now();
    }
}
```

#### 2.1.2 项目配置实体 (ProjectConfig)
```java
@Data
public class ProjectConfig {
    private Long id;
    private String projectId;           // 项目ID
    private String encoding;            // 编码格式
    private String language;            // 使用语言
    private String standardType;        // 标准类型
    private String standardVersion;     // 标准版本
    private String ctVersion;           // CT版本
    private Boolean chineseStandard;    // 中文标准
    private Boolean englishStandard;    // 英文标准
    private String sourceFormat;        // 源文件格式
    private String configuration;       // 配置说明
    private String creator;             // 创建人
    private LocalDateTime createTime;   // 创建时间
    private LocalDateTime updateTime;   // 更新时间
}
```

#### 2.1.3 文件上传实体 (ProjectFiles)
```java
@Data
public class ProjectFiles {
    private Long id;
    private String projectId;           // 项目ID
    private String fileType;            // 文件类型
    private String fileName;            // 原始文件名
    private String filePath;            // 文件存储路径
    private Long fileSize;              // 文件大小
    private String fileMd5;             // 文件MD5值
    private String uploadStatus;        // 上传状态
    private String processStatus;       // 处理状态
    private String errorMessage;        // 错误信息
    private String uploadUser;          // 上传人
    private LocalDateTime createTime;   // 创建时间
    private LocalDateTime updateTime;   // 更新时间
}
```

### 2.2 数据传输对象 (DTO)

#### 2.2.1 项目DTO (ProjectDTO)
```java
@Data
public class ProjectDTO {
    private Long id;
    private String projectId;
    private String projectName;
    private String protocolNumber;
    private String protocolName;
    private String protocolVersion;
    private LocalDate protocolDate;
    private String sponsor;
    private String username;
    private Integer status;
    private String statusText;          // 状态描述
    private LocalDateTime createdTime;
    private LocalDateTime updatedTime;
    private FileStats fileStats;        // 文件统计信息

    @Data
    public static class FileStats {
        private Integer p21SpecCount = 0;
        private Integer xptCount = 0;
        private Integer projectSpecCount = 0;
        private Integer acrfCount = 0;
    }

    public void setStatus(Integer status) {
        this.status = status;
        this.statusText = status == 1 ? "活跃" : "归档";
    }
}
```

#### 2.2.2 统一返回结果 (CommonResult)
```java
@Data
public class CommonResult<T> {
    private boolean success;
    private String code;
    private String message;
    private T data;
    private Long timestamp;

    public CommonResult() {
        this.timestamp = System.currentTimeMillis();
    }

    public static <T> CommonResult<T> success() {
        CommonResult<T> result = new CommonResult<>();
        result.setSuccess(true);
        result.setCode("200");
        result.setMessage("操作成功");
        return result;
    }

    public static <T> CommonResult<T> success(T data) {
        CommonResult<T> result = success();
        result.setData(data);
        return result;
    }

    public static <T> CommonResult<T> fail(String code, String message) {
        CommonResult<T> result = new CommonResult<>();
        result.setSuccess(false);
        result.setCode(code);
        result.setMessage(message);
        return result;
    }
}
```

## 3. 数据访问层设计

### 3.1 MyBatis Mapper接口

#### 3.1.1 项目Mapper (ProjectMapper)
```java
@Mapper
public interface ProjectMapper {

    @Insert("INSERT INTO project (project_id, project_name, protocol_number, protocol_name, " +
            "protocol_version, protocol_date, sponsor, username, status, created_time, updated_time, deleted) " +
            "VALUES (#{projectId}, #{projectName}, #{protocolNumber}, #{protocolName}, " +
            "#{protocolVersion}, #{protocolDate}, #{sponsor}, #{username}, #{status}, #{createdTime}, #{updatedTime}, #{deleted})")
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(Project project);

    @Select("SELECT * FROM project WHERE project_id = #{projectId} AND deleted = 0")
    @Results({
        @Result(property = "projectId", column = "project_id"),
        @Result(property = "projectName", column = "project_name"),
        @Result(property = "protocolNumber", column = "protocol_number"),
        @Result(property = "protocolName", column = "protocol_name"),
        @Result(property = "protocolVersion", column = "protocol_version"),
        @Result(property = "protocolDate", column = "protocol_date"),
        @Result(property = "sponsor", column = "sponsor"),
        @Result(property = "username", column = "username"),
        @Result(property = "status", column = "status"),
        @Result(property = "createdTime", column = "created_time"),
        @Result(property = "updatedTime", column = "updated_time"),
        @Result(property = "deleted", column = "deleted"),
        @Result(property = "id", column = "id")
    })
    Project selectByProjectId(String projectId);

    @Select("SELECT * FROM project WHERE status = 1 AND deleted = 0 ORDER BY created_time DESC")
    @ResultMap("BaseResultMap")
    List<Project> selectActiveProjects();

    @Select("SELECT * FROM project WHERE username = #{username} AND status = 1 AND deleted = 0 ORDER BY created_time DESC")
    @ResultMap("BaseResultMap")
    List<Project> selectActiveProjectsByUsername(String username);

    @Update("UPDATE project SET project_name = #{projectName}, protocol_number = #{protocolNumber}, " +
            "protocol_name = #{protocolName}, protocol_version = #{protocolVersion}, protocol_date = #{protocolDate}, " +
            "sponsor = #{sponsor}, status = #{status}, updated_time = #{updatedTime} WHERE id = #{id}")
    int updateById(Project project);

    @Update("UPDATE project SET deleted = 1, updated_time = NOW() WHERE project_id = #{projectId}")
    int deleteByProjectId(String projectId);

    @Select("SELECT COUNT(1) FROM project WHERE project_id = #{projectId} AND deleted = 0")
    int existsByProjectId(String projectId);

    @Update("UPDATE project SET status = 0, updated_time = NOW() WHERE project_id = #{projectId}")
    int archiveProject(String projectId);

    @Update("UPDATE project SET status = 1, updated_time = NOW() WHERE project_id = #{projectId}")
    int activateProject(String projectId);
}
```

#### 3.1.2 项目配置Mapper (ProjectConfigMapper)
```java
@Mapper
public interface ProjectConfigMapper {

    @Insert("INSERT INTO project_config (project_id, encoding, language, standard_type, " +
            "standard_version, ct_version, chinese_standard, english_standard, source_format, " +
            "configuration, creator, create_time, update_time) " +
            "VALUES (#{projectId}, #{encoding}, #{language}, #{standardType}, #{standardVersion}, " +
            "#{ctVersion}, #{chineseStandard}, #{englishStandard}, #{sourceFormat}, " +
            "#{configuration}, #{creator}, #{createTime}, #{updateTime})")
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(ProjectConfig config);

    @Select("SELECT * FROM project_config WHERE project_id = #{projectId}")
    @Results({
        @Result(property = "projectId", column = "project_id"),
        @Result(property = "standardType", column = "standard_type"),
        @Result(property = "standardVersion", column = "standard_version"),
        @Result(property = "ctVersion", column = "ct_version"),
        @Result(property = "chineseStandard", column = "chinese_standard"),
        @Result(property = "englishStandard", column = "english_standard"),
        @Result(property = "sourceFormat", column = "source_format"),
        @Result(property = "createTime", column = "create_time"),
        @Result(property = "updateTime", column = "update_time")
    })
    ProjectConfig selectByProjectId(String projectId);

    @Update("UPDATE project_config SET encoding = #{encoding}, language = #{language}, " +
            "standard_type = #{standardType}, standard_version = #{standardVersion}, " +
            "ct_version = #{ctVersion}, chinese_standard = #{chineseStandard}, " +
            "english_standard = #{englishStandard}, source_format = #{sourceFormat}, " +
            "configuration = #{configuration}, update_time = #{updateTime} " +
            "WHERE project_id = #{projectId}")
    int updateByProjectId(ProjectConfig config);

    @Delete("DELETE FROM project_config WHERE project_id = #{projectId}")
    int deleteByProjectId(String projectId);
}
```

## 4. 业务逻辑层设计

### 4.1 项目服务接口 (IProjectService)
```java
public interface IProjectService {

    /**
     * 创建项目
     */
    Project createProject(String projectId, String projectName, String protocolNumber,
                         String protocolName, String protocolVersion, String protocolDateStr,
                         String sponsor, String username);

    /**
     * 获取项目信息
     */
    Project getProject(String projectId);

    /**
     * 获取项目DTO信息（包含文件统计）
     */
    ProjectDTO getProjectDTO(String projectId);

    /**
     * 列出活跃项目
     */
    List<ProjectDTO> listActiveProjects();

    /**
     * 根据用户名列出活跃项目
     */
    List<ProjectDTO> listActiveProjectsByUsername(String username);

    /**
     * 更新项目信息
     */
    Project updateProject(String projectId, String projectName, String description);

    /**
     * 删除项目
     */
    boolean deleteProject(String projectId);

    /**
     * 归档项目
     */
    boolean archiveProject(String projectId);

    /**
     * 激活项目
     */
    boolean activateProject(String projectId);

    /**
     * 创建项目文件夹结构
     */
    boolean createProjectDirectories(String projectId);

    /**
     * 检查项目是否存在
     */
    boolean projectExists(String projectId);

    /**
     * 检查用户是否有项目权限
     */
    boolean hasProjectPermission(String projectId, String username);
}
```

### 4.2 项目服务实现 (ProjectServiceImpl)
```java
@Service
@Transactional
public class ProjectServiceImpl implements IProjectService {

    private static final Logger logger = LoggerFactory.getLogger(ProjectServiceImpl.class);

    @Resource
    private ProjectMapper projectMapper;

    @Value("${app.projects.base-path:E:/JAVAPROJ/008_defineXML/projects}")
    private String projectsBasePath;

    private static final String TEMPLATE_PROJECT_ID = "P000_Demo";

    @Override
    public Project createProject(String projectId, String projectName, String protocolNumber,
                                String protocolName, String protocolVersion, String protocolDateStr,
                                String sponsor, String username) {
        logger.info("创建项目: projectId={}, projectName={}, username={}",
                    projectId, projectName, username);

        // 检查项目是否已存在
        if (projectExists(projectId)) {
            throw new RuntimeException("项目ID已存在: " + projectId);
        }

        // 创建项目实体
        Project project = new Project(projectId, projectName);
        project.setProtocolNumber(protocolNumber);
        project.setProtocolName(protocolName);
        project.setProtocolVersion(protocolVersion);

        // 处理日期
        if (protocolDateStr != null && !protocolDateStr.trim().isEmpty()) {
            try {
                DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
                LocalDate date = LocalDate.parse(protocolDateStr, formatter);
                project.setProtocolDate(date);
            } catch (Exception e) {
                logger.warn("日期格式解析失败: {}", protocolDateStr);
            }
        }

        project.setSponsor(sponsor);
        project.setUsername(username);

        // 保存到数据库
        int result = projectMapper.insert(project);
        if (result <= 0) {
            throw new RuntimeException("创建项目失败");
        }

        // 创建项目文件夹结构
        if (!createProjectDirectories(projectId)) {
            logger.warn("创建项目文件夹结构失败，但项目已保存到数据库: {}", projectId);
        }

        logger.info("项目创建成功: {}", project);
        return project;
    }

    @Override
    public boolean createProjectDirectories(String projectId) {
        try {
            Path projectDir = Paths.get(projectsBasePath, projectId);
            Path templateDir = Paths.get(projectsBasePath, TEMPLATE_PROJECT_ID);

            // 如果模板项目存在且当前项目不是模板项目，则从模板复制
            if (!projectId.equals(TEMPLATE_PROJECT_ID) && Files.exists(templateDir)) {
                logger.info("从模板项目复制文件夹结构: {} -> {}", TEMPLATE_PROJECT_ID, projectId);
                copyDirectoryStructure(templateDir, projectDir);
                logger.info("从模板项目复制完成: {}", projectId);
                return true;
            } else {
                // 手动创建文件夹结构
                return createDirectoriesManually(projectDir, projectId);
            }

        } catch (Exception e) {
            logger.error("创建项目文件夹结构失败: {}", projectId, e);
            return false;
        }
    }

    private boolean createDirectoriesManually(Path projectDir, String projectId) throws IOException {
        // 创建项目主目录
        Files.createDirectories(projectDir);
        logger.info("创建项目目录: {}", projectDir);

        // 创建uploads子目录
        String[] uploadDirs = {"p21-spec", "xpt", "project-spec", "acrf"};
        for (String dir : uploadDirs) {
            Path uploadDir = projectDir.resolve("uploads").resolve(dir);
            Files.createDirectories(uploadDir);
            logger.debug("创建上传目录: {}", uploadDir);
        }

        // 创建define子目录
        String[] defineDirs = {"p21空spec", "sdtm define package", "项目Spec", "SDTM注释CRF"};
        for (String dir : defineDirs) {
            Path defineDir = projectDir.resolve("define").resolve(dir);
            Files.createDirectories(defineDir);
            logger.debug("创建define目录: {}", defineDir);
        }

        // 创建output子目录
        String[] outputDirs = {"define", "reports"};
        for (String dir : outputDirs) {
            Path outputDir = projectDir.resolve("output").resolve(dir);
            Files.createDirectories(outputDir);
            logger.debug("创建输出目录: {}", outputDir);
        }

        logger.info("项目文件夹结构手动创建成功: {}", projectId);
        return true;
    }

    private ProjectDTO convertToDTO(Project project) {
        ProjectDTO dto = new ProjectDTO();
        BeanUtils.copyProperties(project, dto);
        dto.setStatus(project.getStatus()); // 这会自动设置statusText
        return dto;
    }

    private void fillFileStats(ProjectDTO dto, String projectId) {
        try {
            ProjectDTO.FileStats stats = new ProjectDTO.FileStats();
            // 这里可以根据实际需要查询各类文件的数量
            stats.setP21SpecCount(0);
            stats.setXptCount(0);
            stats.setProjectSpecCount(0);
            stats.setAcrfCount(0);
            dto.setFileStats(stats);
        } catch (Exception e) {
            logger.warn("获取项目文件统计失败: {}", projectId, e);
            dto.setFileStats(new ProjectDTO.FileStats());
        }
    }
}
```

### 4.3 文件上传服务 (FileUploadService)
```java
@Service
public class FileUploadService {

    private static final Logger logger = LoggerFactory.getLogger(FileUploadService.class);

    @Value("${app.upload.base-path:E:/JAVAPROJ/008_defineXML/projects}")
    private String uploadBasePath;

    @Resource
    private ProjectFilesMapper projectFilesMapper;

    /**
     * 处理文件上传
     */
    public String handleFileUpload(MultipartFile file, String projectId, String fileType, String username) {
        try {
            // 验证文件
            validateFile(file, fileType);

            // 生成文件信息
            String originalName = file.getOriginalFilename();
            String fileName = generateFileName(originalName);
            String filePath = getUploadPath(projectId, fileType, fileName);

            // 保存文件
            Path targetPath = Paths.get(filePath);
            Files.createDirectories(targetPath.getParent());
            file.transferTo(targetPath.toFile());

            // 记录文件信息
            ProjectFiles projectFile = new ProjectFiles();
            projectFile.setProjectId(projectId);
            projectFile.setFileType(fileType);
            projectFile.setFileName(originalName);
            projectFile.setFilePath(filePath);
            projectFile.setFileSize(file.getSize());
            projectFile.setFileMd5(calculateMD5(file));
            projectFile.setUploadStatus("success");
            projectFile.setProcessStatus("pending");
            projectFile.setUploadUser(username);
            projectFile.setCreateTime(LocalDateTime.now());
            projectFile.setUpdateTime(LocalDateTime.now());

            projectFilesMapper.insert(projectFile);

            logger.info("文件上传成功: {}, 项目: {}, 用户: {}", originalName, projectId, username);
            return filePath;

        } catch (Exception e) {
            logger.error("文件上传失败: {}", e.getMessage(), e);
            throw new RuntimeException("文件上传失败: " + e.getMessage());
        }
    }

    private void validateFile(MultipartFile file, String fileType) {
        if (file.isEmpty()) {
            throw new IllegalArgumentException("文件不能为空");
        }

        String originalName = file.getOriginalFilename();
        if (originalName == null) {
            throw new IllegalArgumentException("文件名不能为空");
        }

        // 根据文件类型验证文件扩展名
        String extension = getFileExtension(originalName).toLowerCase();
        switch (fileType) {
            case "acrf":
                if (!extension.equals("pdf")) {
                    throw new IllegalArgumentException("aCRF文件必须是PDF格式");
                }
                break;
            case "p21_spec":
            case "project_spec":
                if (!extension.equals("xlsx") && !extension.equals("xls")) {
                    throw new IllegalArgumentException("Spec文件必须是Excel格式");
                }
                break;
            case "xpt":
                if (!extension.equals("xpt")) {
                    throw new IllegalArgumentException("数据文件必须是XPT格式");
                }
                break;
            default:
                throw new IllegalArgumentException("不支持的文件类型: " + fileType);
        }

        // 文件大小验证（100MB）
        if (file.getSize() > 100 * 1024 * 1024) {
            throw new IllegalArgumentException("文件大小不能超过100MB");
        }
    }

    private String generateFileName(String originalName) {
        String extension = getFileExtension(originalName);
        String timestamp = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMdd_HHmmss"));
        String randomSuffix = UUID.randomUUID().toString().substring(0, 8);
        return timestamp + "_" + randomSuffix + "." + extension;
    }

    private String getUploadPath(String projectId, String fileType, String fileName) {
        return Paths.get(uploadBasePath, projectId, "uploads", fileType, fileName).toString();
    }

    private String getFileExtension(String fileName) {
        int lastDotIndex = fileName.lastIndexOf('.');
        return lastDotIndex > 0 ? fileName.substring(lastDotIndex + 1) : "";
    }

    private String calculateMD5(MultipartFile file) {
        try {
            MessageDigest md = MessageDigest.getInstance("MD5");
            byte[] digest = md.digest(file.getBytes());
            return DatatypeConverter.printHexBinary(digest).toLowerCase();
        } catch (Exception e) {
            logger.warn("计算文件MD5失败", e);
            return null;
        }
    }
}
```

## 5. Web控制层设计

### 5.1 项目管理控制器 (ProjectManagementController)
```java
@RestController
@RequestMapping("/project-management")
public class ProjectManagementController {

    private static final Logger logger = LoggerFactory.getLogger(ProjectManagementController.class);

    @Resource
    private IProjectService projectService;

    /**
     * 创建项目
     */
    @PostMapping("/create")
    public CommonResult<ProjectDTO> createProject(@RequestBody Map<String, Object> request,
                                                  @RequestHeader(value = "username", required = false) String username) {
        logger.info("接收创建项目请求: {}, 用户: {}", request, username);

        try {
            String projectId = (String) request.get("projectId");
            String projectName = (String) request.get("projectName");
            String protocolNumber = (String) request.get("protocolNumber");
            String protocolName = (String) request.get("protocolName");
            String protocolVersion = (String) request.get("protocolVersion");
            String protocolDateStr = (String) request.get("protocolDate");
            String sponsor = (String) request.get("sponsor");

            // 如果请求体中包含username，优先使用请求体中的username
            String requestUsername = (String) request.get("username");
            if (requestUsername != null && !requestUsername.trim().isEmpty()) {
                username = requestUsername;
            }

            if (projectId == null || projectId.trim().isEmpty()) {
                return CommonResult.fail("400", "项目ID不能为空");
            }

            if (projectName == null || projectName.trim().isEmpty()) {
                return CommonResult.fail("400", "项目名称不能为空");
            }

            if (username == null || username.trim().isEmpty()) {
                return CommonResult.fail("401", "用户未登录");
            }

            // 创建项目
            Project project = projectService.createProject(
                projectId, projectName, protocolNumber, protocolName,
                protocolVersion, protocolDateStr, sponsor, username);

            ProjectDTO dto = projectService.getProjectDTO(project.getProjectId());

            logger.info("项目创建成功: {}, 用户: {}", project.getProjectId(), username);
            return CommonResult.success(dto);

        } catch (RuntimeException e) {
            logger.error("创建项目失败: {}", e.getMessage());
            return CommonResult.fail("400", e.getMessage());
        } catch (Exception e) {
            logger.error("创建项目异常", e);
            return CommonResult.fail("500", "创建项目失败: " + e.getMessage());
        }
    }

    /**
     * 获取项目信息
     */
    @GetMapping("/info")
    public CommonResult<ProjectDTO> getProjectInfo(@RequestParam("projectId") String projectId) {
        logger.info("获取项目信息: {}", projectId);

        try {
            ProjectDTO dto = projectService.getProjectDTO(projectId);
            if (dto == null) {
                return CommonResult.fail("404", "项目不存在");
            }

            return CommonResult.success(dto);

        } catch (Exception e) {
            logger.error("获取项目信息失败", e);
            return CommonResult.fail("500", "获取项目信息失败: " + e.getMessage());
        }
    }

    /**
     * 列出当前用户的活跃项目
     */
    @GetMapping("/list/active/my")
    public CommonResult<List<ProjectDTO>> listMyActiveProjects(@RequestHeader(value = "username", required = false) String username) {
        logger.info("获取用户活跃项目列表: {}", username);

        try {
            if (username == null || username.trim().isEmpty()) {
                return CommonResult.fail("401", "用户未登录");
            }

            List<ProjectDTO> projects = projectService.listActiveProjectsByUsername(username);
            logger.info("获取到用户 {} 的 {} 个活跃项目", username, projects.size());
            return CommonResult.success(projects);

        } catch (Exception e) {
            logger.error("获取用户活跃项目列表失败", e);
            return CommonResult.fail("500", "获取项目列表失败: " + e.getMessage());
        }
    }

    /**
     * 删除项目
     */
    @DeleteMapping("/delete")
    public CommonResult<Void> deleteProject(@RequestParam("projectId") String projectId) {
        logger.info("删除项目: {}", projectId);

        try {
            boolean success = projectService.deleteProject(projectId);
            if (success) {
                logger.info("项目删除成功: {}", projectId);
                return CommonResult.success();
            } else {
                return CommonResult.fail("404", "项目不存在或删除失败");
            }

        } catch (Exception e) {
            logger.error("删除项目失败", e);
            return CommonResult.fail("500", "删除项目失败: " + e.getMessage());
        }
    }
}
```

### 5.2 文件上传控制器 (FileUploadController)
```java
@RestController
@RequestMapping("/upload")
public class FileUploadController {

    private static final Logger logger = LoggerFactory.getLogger(FileUploadController.class);

    @Resource
    private FileUploadService fileUploadService;

    /**
     * aCRF文件上传
     */
    @PostMapping("/acrf")
    public CommonResult<String> uploadAcrf(@RequestParam("file") MultipartFile file,
                                          @RequestParam("projectId") String projectId,
                                          @RequestHeader(value = "username", required = false) String username) {
        logger.info("aCRF文件上传: 项目={}, 文件={}, 用户={}", projectId, file.getOriginalFilename(), username);

        try {
            String filePath = fileUploadService.handleFileUpload(file, projectId, "acrf", username);
            return CommonResult.success(filePath);

        } catch (Exception e) {
            logger.error("aCRF文件上传失败", e);
            return CommonResult.fail("500", e.getMessage());
        }
    }

    /**
     * P21 Spec文件上传
     */
    @PostMapping("/p21-spec")
    public CommonResult<String> uploadP21Spec(@RequestParam("file") MultipartFile file,
                                             @RequestParam("projectId") String projectId,
                                             @RequestHeader(value = "username", required = false) String username) {
        logger.info("P21 Spec文件上传: 项目={}, 文件={}, 用户={}", projectId, file.getOriginalFilename(), username);

        try {
            String filePath = fileUploadService.handleFileUpload(file, projectId, "p21_spec", username);
            return CommonResult.success(filePath);

        } catch (Exception e) {
            logger.error("P21 Spec文件上传失败", e);
            return CommonResult.fail("500", e.getMessage());
        }
    }

    /**
     * 项目Spec文件上传
     */
    @PostMapping("/project-spec")
    public CommonResult<String> uploadProjectSpec(@RequestParam("file") MultipartFile file,
                                                 @RequestParam("projectId") String projectId,
                                                 @RequestHeader(value = "username", required = false) String username) {
        logger.info("项目Spec文件上传: 项目={}, 文件={}, 用户={}", projectId, file.getOriginalFilename(), username);

        try {
            String filePath = fileUploadService.handleFileUpload(file, projectId, "project_spec", username);
            return CommonResult.success(filePath);

        } catch (Exception e) {
            logger.error("项目Spec文件上传失败", e);
            return CommonResult.fail("500", e.getMessage());
        }
    }

    /**
     * XPT文件上传
     */
    @PostMapping("/xpt")
    public CommonResult<String> uploadXpt(@RequestParam("file") MultipartFile file,
                                         @RequestParam("projectId") String projectId,
                                         @RequestHeader(value = "username", required = false) String username) {
        logger.info("XPT文件上传: 项目={}, 文件={}, 用户={}", projectId, file.getOriginalFilename(), username);

        try {
            String filePath = fileUploadService.handleFileUpload(file, projectId, "xpt", username);
            return CommonResult.success(filePath);

        } catch (Exception e) {
            logger.error("XPT文件上传失败", e);
            return CommonResult.fail("500", e.getMessage());
        }
    }
}
```

## 6. 配置文件设计

### 6.1 应用配置 (application.yml)
```yaml
server:
  port: 9201
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: define-xml-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/define_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: your_password
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
      enabled: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.stat.common.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true
    lazy-loading-enabled: true
    multiple-result-sets-enabled: true
    use-column-label: true
    use-generated-keys: true
    auto-mapping-behavior: partial

logging:
  level:
    com.stat: DEBUG
    org.springframework.web: DEBUG
  pattern:
    console: '[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%level] [%thread] %logger{36} - %msg%n'
    file: '[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%level] [%thread] %logger{36} - %msg%n'
  file:
    name: logs/define-xml.log
    max-size: 50MB
    max-history: 30

# 应用自定义配置
app:
  projects:
    base-path: E:/JAVAPROJ/008_defineXML/projects
  upload:
    base-path: E:/JAVAPROJ/008_defineXML/projects
    max-file-size: 104857600  # 100MB
    allowed-types:
      acrf: ["pdf"]
      p21_spec: ["xlsx", "xls"]
      project_spec: ["xlsx", "xls"]
      xpt: ["xpt"]
```

### 6.2 CORS配置
```java
@Configuration
public class CorsConfig {

    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.setAllowCredentials(true);
        config.addAllowedOrigin("http://localhost:3000");
        config.addAllowedOrigin("http://127.0.0.1:3000");
        config.addAllowedHeader("*");
        config.addAllowedMethod("*");
        config.setMaxAge(3600L);

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        return new CorsFilter(source);
    }
}
```

### 6.3 异常处理配置
```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    private static final Logger logger = LoggerFactory.getLogger(GlobalExceptionHandler.class);

    @ExceptionHandler(RuntimeException.class)
    public CommonResult<Void> handleRuntimeException(RuntimeException e) {
        logger.error("运行时异常: ", e);
        return CommonResult.fail("500", e.getMessage());
    }

    @ExceptionHandler(IllegalArgumentException.class)
    public CommonResult<Void> handleIllegalArgumentException(IllegalArgumentException e) {
        logger.error("参数异常: ", e);
        return CommonResult.fail("400", e.getMessage());
    }

    @ExceptionHandler(Exception.class)
    public CommonResult<Void> handleException(Exception e) {
        logger.error("系统异常: ", e);
        return CommonResult.fail("500", "系统内部错误");
    }

    @ExceptionHandler(MaxUploadSizeExceededException.class)
    public CommonResult<Void> handleMaxUploadSizeExceededException(MaxUploadSizeExceededException e) {
        logger.error("文件上传大小超限: ", e);
        return CommonResult.fail("400", "文件大小超过限制");
    }
}
```

## 7. 安全设计

### 7.1 用户认证
```java
@Component
public class AuthInterceptor implements HandlerInterceptor {

    private static final Logger logger = LoggerFactory.getLogger(AuthInterceptor.class);

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        String username = request.getHeader("username");

        if (username == null || username.trim().isEmpty()) {
            logger.warn("请求缺少用户身份信息: {}", request.getRequestURI());
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            return false;
        }

        // 设置用户上下文
        UserContext.setCurrentUser(username);
        return true;
    }

    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) {
        UserContext.clear();
    }
}
```

### 7.2 权限控制
```java
@Component
public class ProjectPermissionHelper {

    @Resource
    private IProjectService projectService;

    public boolean hasProjectPermission(String projectId, String username) {
        if (username == null || projectId == null) {
            return false;
        }

        return projectService.hasProjectPermission(projectId, username);
    }

    public void checkProjectPermission(String projectId, String username) {
        if (!hasProjectPermission(projectId, username)) {
            throw new RuntimeException("没有访问该项目的权限");
        }
    }
}
```

## 8. 测试设计

### 8.1 单元测试
```java
@SpringBootTest
@Transactional
@Rollback
public class ProjectServiceTest {

    @Resource
    private IProjectService projectService;

    @Test
    public void testCreateProject() {
        String projectId = "TEST_001";
        String projectName = "测试项目";
        String username = "test_user";

        Project project = projectService.createProject(
            projectId, projectName, null, null, null, null, null, username);

        assertNotNull(project);
        assertEquals(projectId, project.getProjectId());
        assertEquals(projectName, project.getProjectName());
        assertEquals(username, project.getUsername());
    }

    @Test
    public void testProjectExists() {
        String projectId = "TEST_002";
        assertFalse(projectService.projectExists(projectId));

        projectService.createProject(projectId, "测试项目2", null, null, null, null, null, "test_user");
        assertTrue(projectService.projectExists(projectId));
    }
}
```

### 8.2 集成测试
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
public class ProjectManagementControllerTest {

    @Autowired
    private TestRestTemplate restTemplate;

    @Test
    public void testCreateProject() {
        Map<String, Object> request = new HashMap<>();
        request.put("projectId", "TEST_API_001");
        request.put("projectName", "API测试项目");
        request.put("username", "test_user");

        HttpHeaders headers = new HttpHeaders();
        headers.set("username", "test_user");
        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(request, headers);

        ResponseEntity<String> response = restTemplate.postForEntity(
            "/project-management/create", entity, String.class);

        assertEquals(HttpStatus.OK, response.getStatusCode());
    }
}
```

## 9. 性能优化

### 9.1 数据库优化
- 合理设计索引
- 使用连接池管理数据库连接
- 分页查询避免大数据量查询
- 使用缓存减少数据库访问

### 9.2 文件处理优化
- 异步处理大文件上传
- 文件分片上传支持
- 文件压缩和存储优化
- 临时文件清理机制

### 9.3 内存管理
- 合理设置JVM参数
- 监控内存使用情况
- 及时释放大对象引用
- 使用对象池管理频繁创建的对象